import { FC, useState } from 'react';
import { Wrapper } from '../../components';
import DatePicker from 'react-native-date-picker';

export const AddBirthDateScreen: FC = () => {
    const [date, setDate] = useState(new Date());
    return <></>;
};
