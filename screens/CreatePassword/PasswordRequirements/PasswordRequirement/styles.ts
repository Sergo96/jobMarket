import styled from 'styled-components';
import { View } from 'react-native';

export const PasswordRequirementStyled = styled(View)`
    flex-direction: row;
    align-items: center;
    justify-content: flex-start;
`;
